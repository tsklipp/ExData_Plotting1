## First analysis graph

# Construct the plot
  hist(houseHoldConsumption$Global_active_power, breaks = 13, col = "red", 
       main = "Global Active Power", xlab = "Global Active Power (kilowatts)")